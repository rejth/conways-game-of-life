pre-commit:
  parallel: true
  commands:
    biome:
      run: pnpm biome check .
    oxlint:
      run: pnpm oxlint .
    rustfmt:
      run: cargo fmt --manifest-path packages/rust/Cargo.toml --check
